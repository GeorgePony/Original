意图：定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式让其创建过程延迟到子类进行。
关键代码：创建过程在各个子类中进行
优点：
   1）一个调用者想创建一个对象，只要知道其名字就行了。
   2）拓展性高，如果想增加一个产品，只要拓展一个工厂类就行了。
   3）屏蔽产品的具体实现，调用者只关心产品的接口。

缺点：
   1）每次增加一个产品时，都需要增加一个类和对象实现工厂，使得系统中工厂类个数成倍增加。